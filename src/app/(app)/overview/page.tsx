export default function OverviewPage() {
  // @slots not go the Overview page
  // they pass like params to the Layout.tsx
  // However we need to have routing to /overview url to see Layout.tsx
  // Then we have to leave empty component
  return null;
}
